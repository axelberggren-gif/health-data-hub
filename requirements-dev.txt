# Dev + CI tooling. Install with: pip install -r requirements.txt -r requirements-dev.txt
# (or `make install`). Runtime deps stay in requirements.txt.

ruff>=0.6           # linter + formatter (the `make lint` / `make format` engine)
mypy>=1.11          # static type checker (`make typecheck`)
pytest>=8.0         # test runner (`make test`)
httpx>=0.27         # also used by Starlette's TestClient in tests
pip-audit>=2.7      # dependency vulnerability scan (`make audit`)
